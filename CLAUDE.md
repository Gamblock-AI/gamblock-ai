@./AGENTS.md

# Claude Code workspace context

Use `context/README.md` to load task-relevant context. When working inside a
component, also load that component's `AGENTS.md`; descendant instructions are
not a substitute for the self-contained component context in a standalone
clone.
